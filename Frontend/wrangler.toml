# ===========================================
# wrangler.toml - Cloudflare Pages (Frontend Angular)
# ===========================================

name = "gestion-estudiantil"
compatibility_date = "2025-04-04"

# This tells Wrangler to deploy to Cloudflare Pages
# The output directory where the build artifacts are located
pages_build_output_dir = "./dist/cloudflare/browser"

[vars]
API_URL = "/api"
ENVIRONMENT = "production"
